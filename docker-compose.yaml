version: '3.9'
services:
  document-ide:
    container_name: document-ide
    build:
      context: .
      dockerfile: trae-agent/Dockerfile
    ports:
      - "8090:8090"
    environment:
      # Connect to host machine's 5432 port via host.docker.internal
      POSTGRES_URL: postgresql+psycopg://postgres:postgres@host.docker.internal:5432/trae
      DEFAULT_PROVIDER: openrouter
      DEFAULT_MODEL: Qwen3-32B
      OPENROUTER_BASE_URL: http://10.0.2.22:9997/v1
      OPENROUTER_API_KEY: aiaiaiai123
    volumes:
      - /Users/stylesu/Documents/Heils/Agent/workspace:/workspace
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
